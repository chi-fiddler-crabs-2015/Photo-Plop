<%= form_for :album, method: :put, url: album_path(params[:id]) do |f| %>
  <%= render "shared/errors" %>
  <%= render "shared/errors" %>
  <%= f.label :title %>
  <%= f.text_field :title, :required => true %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.label :password %>
  <%= f.text_field :password, placeholder: "(Optional)" %>

  <%= f.label :read_privilege, "Anyone can see" %>
  <%= f.radio_button :read_privilege, "1" %>

  <%= f.label :read_privilege, "Need password to see" %>
  <%= f.radio_button :read_privilege, "2" %>

  <%= f.label :write_privilege, "Anyone can upload images" %>
  <%= f.radio_button :write_privilege, "1" %>

  <%= f.label :write_privilege, "Need password to upload images" %>
  <%= f.radio_button :write_privilege, "2" %>

  <%= f.submit "Update" %>
<% end %>

<% @album.images.each do |image| %>
  <%= cl_image_tag(image.location) %>
  <%= button_to "DELETE", {:controller => :images,
      :action => 'destroy', album_id: @album.id, :id => image.id }, :method => :delete %>
  <br>
<% end %>

<%= button_to "DELETE ALBUM", @album, method: :delete %>


<<<<<<< HEAD
<div class="album_show">
  <div class="row">
=======
<h1><%= @album.title %></h1>
<p><%= @album.description %></p>
<p>Created By: <%= @album.creator.username %></p>

<div id="favorite-button-container">
  <% if current_user %>
    <%= render partial: "favorites/button"%>
  <% end %>
</div>
>>>>>>> 70753bee747d3619e703b36aa4b8ac9d84d0d91b

    <div class="small-9 medium-10 large-10 columns">
      <h1><%= @album.title %></h1>
      <p>Description: <%= @album.description %></p>
      <p>Creator: <%= @album.creator.username %></p>
    </div>

    <div class="small-3 medium-2 large-2 columns">
      <span id="upload-photo-box">
        <%= link_to raw("<i class=\"fa fa-plus-square\"></i>"), new_album_image_path(@album.id), remote: true, id: "upload_button" %>
      </span>

      <span id="favorite-button-container">
        <% if current_user %>
          <%= render partial: "favorites/button"%>
        <% end %>
      </span>

      <a href='' id="share-button"><i class="fa fa-share-alt-square"></i></a>

      <% if @album.owner?(current_user) %>
        <%= link_to raw("<i class=\"fa fa-pencil-square\"></i>"), edit_album_path(@album.id) %>
      <% end %>

    </div>

  </div>

  <div id="share-url-container" style="display: none;" class="row">
    Your album's URL is: <span><input type="text" value="<%= show_vanity_url(@album) %>" disabled></input></span>
    <img src="http://media.idownloadblog.com/wp-content/uploads/2013/04/Twitter-2.2-Mac-app-icon-small.png">
    <img src="http://www.waterproof-usa.com/_images/common/instagram.png">
  </div>

  <ul class="small-block-grid-3 medium-block-grid-5 large-block-grid-6">
    <% @album.images.each do |image| %>
       <li class="img-tile">
        <% if image.owner?(current_user) %>
          <%= button_to "DELETE", {:controller => :images,
          :action => 'destroy', album_id: @album.id, :id => image.id }, :method => :delete %>
        <% end %>
        <%= link_to image_tag(image.location, :type => "fetch", :width => 200, :height=> 200, :crop => :thumb), album_image_path(@album.id, image.id) %>
      </li>
    <% end %>
  </ul>

</div>

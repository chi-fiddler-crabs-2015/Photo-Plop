<%= form_for :album, remote: true, html: { id: "image-password-form" }, url: album_imageauth_path(@album) do |f| %>
  <%= f.label :password %>
  <%= f.text_field :password, value: '' %>

  <%= f.submit "Log Into Album" %>
<% end %>